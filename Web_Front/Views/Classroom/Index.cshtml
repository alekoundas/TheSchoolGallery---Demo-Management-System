@model IEnumerable<Web_DomainClasses.Entities.School.Classroom>

@{
    ViewBag.Title = "Index";
}


<div class="col-md-12 pt-20 pb-20">
    <h1 style="color:#f3efe9;">Classes</h1>
</div>
<div class="feature-area pt-10 pb-10  relative">
    <div class="container">
        <div class="row">
            <h2 class="col-md-6">
                <i class="fa fa-plus-circle" aria-hidden="true"></i>
                @Html.ActionLink("Create New", "Create")
            </h2>
            <div class="col-md-6">
                @using (Html.BeginForm())
                {
                    <p>
                        Find by Name: @Html.TextBox("SearchString")
                        <input class="btn btn-large" type="submit" value="Search" />
                    </p>
                }
            </div>
        </div>
    </div>
</div>

<section class="feature-area pt-100 pb-100  relative">
    <div class="overlay overlay-bg"></div>
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-6 col-sm-12 align-items-stretch">
                    <div class="dottedsurr">
                        <div class="dottedFrameDark2 isThisLink">
                            <br />
                            <a href="@Url.Action("Details", "Classroom", new { id = item.ClassroomId })">
                                <div class="Schooldesc">
                                    <h3>@Html.DisplayFor(modelItem => item.Name)</h3>
                                    <hr />
                                    <ul class="text-right paintingInfo">
                                        <li>Description: @Html.DisplayFor(modelItem => item.Description)</li>
                                    </ul>
                                </div>
                            </a>
                        </div>

                        @if (User.IsInRole("Admin"))
                        {
                            <!-- Section - Links ---------------------------------------->
                            <div style="height:180px;" class="container col-12">
                                <br />
                                <div class="row">
                                    <div class="btn_contain1 col">
                                        <div class="btn_child_norm1">
                                            <a href="@Url.Action("Edit", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/edit_off.png" /></a>
                                        </div>
                                        <div class="btn_child_live1">
                                            <a style="color:#f3efe9" href="@Url.Action("Edit", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/edit.png" /> edit </a>
                                        </div>
                                    </div>

                                    <div class="btn_contain2 col">
                                        <div class="btn_child_norm2">
                                            <a href="@Url.Action("Details", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/details_off.png" /></a>
                                        </div>
                                        <div class="btn_child_live2">
                                            <a style="color:#f3efe9" href="@Url.Action("Details", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/details.png" /> details </a>
                                        </div>
                                    </div>

                                    <div class="btn_contain3 col">
                                        <div class="btn_child_norm3">
                                            <a href="@Url.Action("Delete", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/delete_off.png" /></a>
                                        </div>
                                        <div class="btn_child_live3">
                                            <a style="color:#f3efe9" href="@Url.Action("Delete", "Classroom", new { id = item.ClassroomId })"><img src="~/images/UIcons/delete.png" /> delete </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }
        </div>
    </div>
</section>









